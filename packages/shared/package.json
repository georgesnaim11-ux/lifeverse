{
  "name": "@lifeverse/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types, enums, and game constants for LifeVerse.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch --preserveWatchOutput",
    "clean": "tsc -b --clean"
  }
}
